# 2026-09-09 개발 상태

## 이번 변경

- 관계 지도·관측 근거·AI 서버가 동일한 `relationship-data.js` 정규화 모듈을 사용한다.
- 월별 최신 분석 대상 응답만 사용한다. 같은 응답의 동일 대상 중복은 마지막 유효 1~5 정수 점수 하나를 사용한다.
- 자기 자신, 현재 명단 밖 대상/응답자, 잘못된 월과 점수, 분석 제외·삭제 자료는 관계 계산에서 제외한다. 원본 데이터는 변경하지 않는다.
- 지도와 근거 표의 중복 자료 평균 불일치(5점+1점이 지도 3점, 표 1점)를 동일한 1점·1건으로 수정했다.
- AI 학생 지원과 관계 코칭에 선택 월/선택 월까지 누적 관측 근거를 분리해 전달한다. 응답자 수, 응답 건수, 양방향 공통 관측 및 높은 점수 반복 월 수를 포함한다.
- 관측 부족을 관계 해석에 반영하도록 지시한다. 직접 도움 요청·폭력 서술 확인은 관측 부족과 무관하게 유지한다. 이 지침은 생성 결과의 정확성을 보증하지 않는다.
- 기존 저장 결과에는 최신 버전을 덧씌우지 않는다. 버전 없는 결과는 이전 저장 형식으로 표시하며 새 분석을 자동 실행하지 않는다.
- 모델 `gpt-5.6-terra`, low 추론, 기존 Supabase 키를 유지한다. 키의 값을 읽거나 변경하지 않는다.

## 검증과 배포 구분

- 로컬 기준 Node 192개, 브라우저 6개 검증 대상이다. 완료 상태는 PR 자동 검사 결과와 배포 검증 결과를 함께 확인한다.

- Node 테스트에는 공유 정규화, 지도/표 일치, 미응답, 원본 보존, 과거 캐시, 학급·관계 AI 요청 본문 통합 테스트를 포함한다.
- API 통합 테스트는 실제 Edge Function 본문을 TypeScript 구문 제거 후 실행하고 DB/OpenAI만 가짜 응답으로 대체한다. 외부 AI 비용과 실제 학생 자료 전송이 없다.
- 브라우저 테스트는 가상 자료로 360/768/1440px의 로그인·학생 탐색·관계 근거·월 전환을 확인한다.
- 웹은 PR 검사·main 병합 후 GitHub Pages 배포와 배포 주소 회귀 테스트를 확인한다.
- Supabase는 `analyze-class` 별도 배포가 필요하다. `index.ts`, `privacy.mjs`, `relationship-data.js`를 함께 배포한다. SQL 변경은 없다.
- 실제 운영 AI 새 분석의 문장 품질, 학교 태블릿 동시 제출·잠금·재접속·PDF는 이번 자동 검증에 포함되지 않는다.

## 남은 과제

1. 전입·전출의 실제 재적 기간을 반영한 관측 분모와 안정 ID 일관성 강화.
2. 연결 묶음 기준점 민감도 및 월별 변화의 비교 가능성 강화.
3. 교사가 새 AI 결과의 근거·문장 품질을 확인하는 현장 평가.
4. 실제 태블릿 3~5대 동시 제출 및 학교 PDF 다운로드 검증.

이 문서는 9월 8일 문서의 미배포 안내와 테스트 수를 현재 상태로 오해하지 않도록 대체하는 기준이다. 배포 완료 여부는 해당 PR과 GitHub Actions 및 Supabase 함수 목록의 실제 결과를 기준으로 한다.
