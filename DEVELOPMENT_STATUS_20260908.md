# 2026-09-08 개발 점검

기능 상태는 구현, 자동 검증, 운영 적용, 학교 현장 검증으로 구분한다.

| 항목 | 구현·자동 검증 | 운영·현장 구분 |
|---|---|---|
| AI 서술 이름 처리 | 전송 직전 현재·과거 명단의 이름 치환, 긴 이름 우선, 동명이인 구분 불가 표시, 근거 ID 보존 | Edge Function 별도 배포 필요. 기존 저장 결과는 자동 재작성하지 않음 |
| 최근 12개월 | 한국 시간 현재 월 기준 연속 12개월, 학급 전체 미제출 월도 유지 | 미래 응답은 타임라인 범위 밖 |
| 확인 상태 | 기록의 응답 기준 월과 상태로 예정·완료 구분 | 완료일이 아닌 기준 월 표시 |
| 화면 회귀 | 가상 API 응답으로 로그인·학생 선택·탭·한 줄 배치·넘침 검사 | 실제 DB 권한·학교 기기 검증을 대신하지 않음 |

Node 테스트 174개와 Chromium 360·768·1440px 검사 3개가 로컬에서 통과했다. CI에서도 두 검사군을 실행한다.

## 이름 처리 범위

현재 명단 및 과거 응답자 이름을 사용해 서술 전체에서 이름을 치환한다. 명단에 없는 별명, 이름 일부, 외부 인물 이름과 연락처를 모두 알아내는 완전 익명화 기능은 아니다. PRD의 익명 번호 전송은 이 제한을 포함한 가명 처리로 이해해야 한다. 교사 화면의 원문과 저장된 응답은 변경하지 않는다.

## 실행

`npm ci`, `npm test`, `npx playwright install chromium`, `npm run test:browser` 순서로 검증한다. TEST_BASE_URL 환경변수로 배포 주소를 지정할 수 있으며 DB 요청은 가상 응답으로 대체된다.

Edge Function은 `npx supabase functions deploy analyze-class --project-ref wxhcualawzwybsllrkqm`로 별도 배포한다. index.ts와 privacy.mjs 두 파일을 함께 배포해야 한다. 기존 서버 비밀값을 유지한다.

## 남은 현장 검증

태블릿 3~5대 동시 제출, 잠금·회전·재연결과 학교 PDF 검증은 이번 요청에서 제외했다. 현장 시험 완료로 표시하지 않는다. 과거 문서의 기준 커밋은 당시 검증 이력이며 현재 배포 상태는 GitHub Actions에서 확인한다.
