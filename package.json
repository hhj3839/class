{
  "name": "class-ieum",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
