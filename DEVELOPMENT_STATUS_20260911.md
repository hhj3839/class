# 2026-09-11 운영 안정화

## 완료 범위

- 기존 OPENAI_API_KEY 재사용 승인. 키 원문 조회·변경과 실학생 유료 AI 호출 없이 작업.
- 기본 Chromium 실행은 spawn UNKNOWN으로 실패한다. 설치된 Edge의 빈 브라우저 실행과 11개 화면 검사는 통과했다. Chromium 자체 복구라고 주장하지 않으며 자동 fallback으로 오류를 숨기지 않는다.
- PowerShell: `$env:TEST_BROWSER_CHANNEL='msedge'`와 `$env:TEST_BASE_URL='https://class-ieum.vercel.app/'` 설정 후 `npx playwright test`를 실행한다. 환경 변수를 생략하는 CI는 기본 Chromium을 사용한다.
- 학생 코칭 API 오류를 코드에 따라 구분한다. insufficient_quota는 결제·할당량 확인, rate_limit_exceeded는 일시 제한 안내, 불명확한 429는 원인 미확인으로 표시한다. 공급자 오류 원문은 노출하지 않는다.
- 키 인증, 모델/프로젝트 접근, 외부 서비스 오류를 별도 안내한다. 자동 유료 재시도 및 앱 한도 차감 정책 변경 없음.
- Node 221개, Edge 화면 11개 통과. 모의 API 오류는 실제 Edge Function 코드 경로를 거친다.
- PRD에 코칭 탭·전출·월 100회·현재 UI·미완료 코칭 백업/보존 범위를 명시했다.

## 남은 작업

1. 학교 태블릿 3~5대 동시 제출·회전·잠금·재연결·학교 PDF 다운로드 실측은 별도 현장 과제.

## 학생 코칭 PDF

- 학생별 살펴보기의 기존 PDF 저장 버튼에 코칭 섹션을 추가했다. 별도 AI 생성 없이 action=load로 저장 결과만 확인한다.
- 코칭 초점·질문·지도 방법·후속 확인·강점/어려움·주의사항·연결 근거·현재 카드의 최신 적용 결과를 출력한다.
- 오래된/삭제된 카드 제외, 이름 제외 시 코칭 전체 제외, 원문 제외 시 문항 정보만 출력. 조회 중 로그아웃·학급 변경 시 저장 중단.
- Node 224개와 화면 11개 통과. 가상 학생의 실제 다운로드 PDF 3페이지를 PDFium으로 렌더링해 한글·여백·페이지 번호·잘림을 확인했다. Poppler는 로컬 번들에서 찾지 못해 PDFium을 사용했다.
- 학년 말 일괄 PDF는 이번 변경 범위에 포함하지 않았다. 운영 DB·Edge Function 재배포는 필요하지 않다.

## 코칭 백업·정리 연계

- `20260911150000_coaching_backup_retention.sql`: 기존 백업 형식에 코칭 카드·적용 결과를 추가한다. 기존 백업도 복구 가능하다.
- 같은 학급의 기존 코칭은 덮어쓰지 않아 삭제된 카드가 이전 백업으로 부활하지 않는다. 다른 학급은 학생·카드·적용 결과 UUID를 분리하고 자료 해시를 무효화한다. 원래 근거 ID는 과거 기록으로만 보존되며 현재 자료의 코칭으로 표시하지 않는다. 새 분석은 교사가 직접 요청한다.
- 생성 중 요청은 다른 학급 복구 시 실패 상태로 변환하여 영구 대기를 막는다. 원래 생성 시각과 생성 이력은 보존한다.
- 학년 말 집계·변경 지문에 코칭과 적용 결과를 포함한다. 기존 학생 삭제의 외래키로 함께 정리하며, 기존 학년도·확인 문구 검사를 유지한다. 자동 삭제는 추가하지 않는다.
- 기존 함수는 권한이 제거된 내부 함수로 보존한다. 외부에는 인증·학급 권한을 확인하는 기존 RPC 이름만 공개한다.
- 가상 학급 롤백 테스트로 백업·동일/타 학급 복구·삭제 자료 부활 방지·권한·변경 후 삭제 차단·가상 학급 정리를 검증한다. 실제 운영 자료 삭제는 실행하지 않는다.
- 2026-09-11 운영 SQL 적용과 적용 전/후 롤백 검증 완료. 시험 학급은 모두 롤백되었다.

배포 완료 여부는 PR 검사·main 병합·Pages 결과와 Supabase 함수 목록을 기준으로 확인한다. 외부 AI 잔액이나 계정 한도 자체를 해결했다고 주장하지 않는다.
