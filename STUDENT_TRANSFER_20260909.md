# 학생 전출 처리

## 사용 방법

학급 설정 → 학생 명단 → 전출 → 날짜 확인 → 전출 처리하기.
기본 날짜는 한국 시간 오늘이며, 과거 날짜로 변경할 수 있다. 미래 예약은 지원하지 않는다.
잘못 처리한 경우 같은 위치의 전출 취소 버튼으로 복원한다.
학생별 살펴보기는 재학 학생만 기본 표시하며, 전출 학생 포함을 켜면 과거 응답과 PDF를 확인할 수 있다. 이전·다음 이동도 표시된 명단을 따른다.
제출현황 이름은 전출월부터 `이름(전출)`로 표시한다. 전출 이전 월의 이름과 참여 집계는 유지한다.
DB에 저장하지 않은 학생은 명단을 먼저 저장해야 한다.

## 보존과 집계

- 학생 행과 기존 설문·관찰 기록을 삭제하지 않는다. 명단에 전출일을 표시한다.
- DB active=false로 학생 설문 이름 선택과 제출을 차단한다. 명단 동기화로 재활성화되지 않도록 트리거가 보호한다.
- 전출 이전 월은 이전대로 집계한다. 전출월 미참여는 일부 기간 재적으로 분리하며 미제출에 포함하지 않는다. 전출월 제출자는 제출 인원과 집계 대상에 포함한다.
- 전출일이 1일이면 해당 월 전체를 전출 상태로 취급한다. 그 이후 달은 참여 집계에서 제외한다.
- 관계 변화는 전출월부터 비교를 보류한다. 누적 관계망의 과거 응답은 유지한다.
- 전출·취소는 담당 교사만 실행할 수 있으며 감사 로그를 남긴다.

## 검증과 배포

- Node 자동 테스트 204개, 브라우저 테스트 8개.
- 운영 DB에서 승인받은 가상 레코드 롤백 검증: 전출·취소, 참여 차단, 신원·권한 확인, 미래 날짜 차단, 명단 재동기화, 과거 응답과 감사 기록 보존.
- 20260909120000_student_transfer_status.sql 운영 DB 적용 완료. 교사가 SQL을 다시 실행할 필요 없다.
- Edge Function 변경 없음.

## 남은 범위

전입일, 재전입에 따른 여러 재적 구간, 기존 백업 파일을 통한 전출일 복원 및 AI의 재적 기간 전용 해석은 이번 변경 범위가 아니다. 전출 취소는 원래 처리를 되돌리는 기능이지 재전입 등록 기능이 아니다.
