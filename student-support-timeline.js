const studentSupportFilters={month:'all',kind:'all'};
let studentSupportFilterStudent=null;

function studentSupportItems(student){
  const number=Number(student.number),studentId=student.studentId||null,items=[];
  studentMonthlyResponses(number).forEach(({month,item})=>items.push({kind:'survey',date:item.submitted_at||`${month}-01`,label:'설문 제출',title:`${monthLabel(month)} 학교생활 돌아보기`,detail:payloadOf(item).helpNow||'도움 요청 없음',tone:/바로|즉시/.test(payloadOf(item).helpNow||'')?'urgent':'survey'}));
  signalReviews.filter(review=>(studentId&&review.student_id===studentId)||Number(review.student_number)===number).forEach(review=>items.push({kind:'signal',date:review.updated_at||review.follow_up_date||'',label:'확인이 필요한 응답',title:review.source_snapshot?.title||'응답 확인 항목',detail:signalReviewStatuses[review.status]||review.status,tone:['support_connected','no_issue','closed'].includes(review.status)?'done':review.status==='unreviewed'?'urgent':'progress',followUpDate:review.follow_up_date,note:review.note}));
  getObservations().filter(item=>(studentId&&item.studentId===studentId)||Number(item.studentNumber)===number).forEach(item=>items.push({kind:'observation',id:item.id,date:item.followUpDate||item.date||'',label:'교사 확인 기록',title:item.title,detail:item.status==='done'?({support:'지원 연결',no_issue:'특이사항 없음',continue:'계속 관찰'}[item.outcome]||'확인 완료'):item.status==='doing'?'확인 중':'확인 예정',tone:item.status==='done'?'done':'progress',note:item.observedFact||item.detail}));
  return items.sort((a,b)=>String(b.date).localeCompare(String(a.date)));
}

function studentSupportItemHTML(item){return`<article class="student-support-item ${item.tone}"><span class="support-dot"></span><div><header><span>${escapeHTML(item.label)}</span><time>${escapeHTML(String(item.date||'날짜 미정').slice(0,10))}</time></header><h4>${escapeHTML(item.title)}</h4><p><strong>${escapeHTML(item.detail||'')}</strong>${item.note?`<br>${escapeHTML(item.note)}`:''}</p>${item.kind==='observation'?`<button class="text-button" type="button" data-edit-observation="${item.id}">관찰 기록 열기</button>`:''}</div></article>`}

function filteredStudentSupportItems(items){return items.filter(item=>(studentSupportFilters.month==='all'||String(item.date||'').slice(0,7)===studentSupportFilters.month)&&(studentSupportFilters.kind==='all'||item.kind===studentSupportFilters.kind))}

function buildStudentSupportReportSection(student){
  const items=studentSupportItems(student),recent=items.slice(0,8),counts={survey:items.filter(item=>item.kind==='survey').length,signal:items.filter(item=>item.kind==='signal').length,observation:items.filter(item=>item.kind==='observation').length},open=items.filter(item=>(item.kind==='signal'||item.kind==='observation')&&item.tone!=='done').length;
  return`<section class="pdf-ai-section"><div class="pdf-block pdf-section-heading"><h2>확인·지원 이력 요약</h2><p>설문 ${counts.survey}건 · 확인이 필요한 응답 ${counts.signal}건 · 교사 확인 기록 ${counts.observation}건 · 확인 진행 중 ${open}건</p></div>${recent.length?recent.map(item=>`<article class="pdf-block pdf-support-item"><h4>${escapeHTML(String(item.date||'날짜 미정').slice(0,10))} · ${escapeHTML(item.label)}</h4><p><b>${escapeHTML(item.title)}</b><br>${escapeHTML(item.detail||'')}${item.note?`<br>${escapeHTML(item.note)}`:''}</p></article>`).join(''):'<p class="pdf-block">연결된 확인·지원 기록이 없습니다.</p>'}</section>`
}

function renderStudentSupportTimeline(){
  const select=$('#studentDetailSelect'),content=$('#studentDetailContent');if(!select?.value||!content)return;
  const student=classSettings.students.find(row=>Number(row.number)===Number(select.value));if(!student)return;
  if(studentSupportFilterStudent!==Number(student.number)){studentSupportFilterStudent=Number(student.number);studentSupportFilters.month='all';studentSupportFilters.kind='all'}
  const items=studentSupportItems(student),months=[...new Set(items.map(item=>String(item.date||'').slice(0,7)).filter(month=>/^\d{4}-\d{2}$/.test(month)))].sort().reverse(),visibleItems=filteredStudentSupportItems(items),openSignals=signalReviews.filter(review=>((student.studentId&&review.student_id===student.studentId)||Number(review.student_number)===Number(student.number))&&!['support_connected','no_issue','closed'].includes(review.status)),openObservations=getObservations().filter(item=>Number(item.studentNumber)===Number(student.number)&&item.status!=='done'),today=new Date().toISOString().slice(0,10),nextDate=[...openSignals.map(item=>item.follow_up_date),...openObservations.map(item=>item.followUpDate)].filter(date=>date&&date>=today).sort()[0];
  const panel=document.createElement('section');panel.className='panel student-support-panel';panel.innerHTML=`<div class="panel-head"><div><p class="eyebrow">응답 → 확인 → 지원</p><h3>확인·지원 이력</h3></div><span class="confidence ${openSignals.length+openObservations.length?'':'good'}">확인 진행 중 ${openSignals.length+openObservations.length}건</span></div><div class="student-support-summary"><article><span>설문 제출</span><strong>${items.filter(item=>item.kind==='survey').length}</strong></article><article><span>확인이 필요한 응답</span><strong>${items.filter(item=>item.kind==='signal').length}</strong></article><article><span>교사 확인 기록</span><strong>${items.filter(item=>item.kind==='observation').length}</strong></article><article><span>다음 확인 예정일</span><strong>${nextDate||'미정'}</strong></article></div><div class="student-support-filters"><label><span>월</span><select data-support-filter="month"><option value="all">모든 월</option>${months.map(month=>`<option value="${month}" ${studentSupportFilters.month===month?'selected':''}>${monthLabel(month)}</option>`).join('')}</select></label><label><span>기록 종류</span><select data-support-filter="kind"><option value="all">모든 기록</option><option value="survey" ${studentSupportFilters.kind==='survey'?'selected':''}>설문 제출</option><option value="signal" ${studentSupportFilters.kind==='signal'?'selected':''}>확인이 필요한 응답</option><option value="observation" ${studentSupportFilters.kind==='observation'?'selected':''}>교사 확인 기록</option></select></label><span class="muted">${visibleItems.length}건 표시</span></div><div class="student-support-list">${visibleItems.length?visibleItems.map(studentSupportItemHTML).join(''):'<p class="muted">선택한 조건에 해당하는 기록이 없습니다.</p>'}</div></section>`;
  const slot=content.querySelector('#studentSupportTimelineSlot');if(slot)slot.replaceChildren(panel);else content.append(panel);
  panel.querySelectorAll('[data-support-filter]').forEach(select=>select.addEventListener('change',event=>{studentSupportFilters[event.target.dataset.supportFilter]=event.target.value;renderStudentSupportTimeline()}));
}

$('#studentDetailSelect').addEventListener('change',renderStudentSupportTimeline);
document.addEventListener('class-ieum:data-updated',renderStudentSupportTimeline);
