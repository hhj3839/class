# 가상 학급 둘러보기

로그인 화면 아래의 버튼으로 가상 학급을 조회한다. 브라우저에 실험실 교사 비밀번호·로그인 토큰을 전달하지 않는다. 체험 상태는 메모리에서만 유지되며 새로고침·종료 시 끝난다.

## 허용 범위

- 고정 실험실 `demo-aa891ab949014621`의 명단·설문·저장된 AI 결과 조회와 PDF.
- 학급·관계 AI 새 분석 합산 월 10회, 학생 코칭 월 100회: 전체 게스트 공용 상한이다.
- 한국 시간 기준 월초 초기화. 실패 요청도 포함하며 조회는 무료다. 기존 실험실 코칭 자체 한도가 먼저 소진되면 생성이 더 일찍 제한될 수 있다.
- 수정·삭제·회원 관리·QR 제출 링크·백업·보존 정책 변경은 공개하지 않는다.

## 보안 경계

공개 `guest-lab` Edge Function만 JWT 없이 동작한다. 기존 교사용 함수의 인증은 변경하지 않는다. 서버는 허용된 RPC·AI 함수만 호출하고 학급 ID를 고정하며 클라이언트가 보낸 임의 필드는 버린다. 실험실 소유 계정을 매번 DB에서 확인하고, 서비스 전용 원자적 카운터로 상한을 집행한다. 참여 토큰은 응답에서 제거한다.

서버 내부 로그인은 Supabase 관리 API의 링크 생성·검증을 사용하며 메일은 발송하지 않는다. 토큰은 서버 메모리에만 잠시 유지한다. 실험실에는 앞으로도 **실제 학생·교사 개인정보를 입력하지 않아야 한다**. 공개 체험 결과는 방문자 모두에게 보인다.

## 운영

`supabase/migrations/20260915120000_guest_lab_quota.sql` 적용 후 `guest-lab`을 `--no-verify-jwt`로 배포한다. 다른 함수에 이 옵션을 적용하지 않는다. `supabase/demo/test_guest_lab_quota.sql`은 한도 경계와 비로그인 거부를 검증한 뒤 전부 롤백한다. DB/설정 누락 시 공개 진입은 실패하도록 구현했다.

가상 자료의 월 갱신은 기존 실험실 교사 로그인 때 실행된다. 게스트 진입 자체는 자료를 재작성하지 않는다. 공개 기능 중단 시 `guest-lab` 배포를 비활성화하면 된다.

## 검증

- 자동 테스트 293개, 브라우저 테스트 17개 통과.
- 운영 DB의 10·100회 경계 및 익명 호출 거부를 롤백 검증했다.
- 운영 게이트웨이의 가상 명단·저장 코칭 조회, 참여 토큰 미노출, 삭제 요청 거부를 확인했다.
- 이번 작업에서 실제 유료 생성은 실행하지 않았다. 저장 코칭 조회 시 실험실 계정 잔여 83회, 게스트 카운터 100회를 확인했다.
