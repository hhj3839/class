# 학생 코칭 카드

## 사용 흐름

학생별 살펴보기 → 학생 선택 → 학생 코칭 → 코칭 카드 만들기.
탭 조회는 저장 결과만 불러오며, 버튼을 누르고 전송을 확인해야 유료 AI 생성이 시작된다.
강점·살펴볼 점, 개방형 질문, 지도 방법 1~2개, 지도 후 확인할 내용을 표시한다. 각 제안의 근거 버튼으로 문항과 학생 응답·교사 기록 또는 계산값을 확인한다.
교사는 아직 시도 전 / 도움 됨 / 다른 방법 필요와 메모를 저장할 수 있다. 다시 만들 때 저장된 적용 결과를 참고한다.

## 자료·안전 기준

- 최근 12개월 자료 중 본인의 최신 4회 설문, 교사 관찰·면담 최대 6회, 관계 평가 계산값을 최대 60개 근거로 사용한다.
- 학생 고유 ID가 확인되는 본인 응답만 사용한다. 다른 학생의 무관한 자유서술은 AI 입력에서 제외한다.
- 관계 점수는 같은 달 본인의 과거 번호가 확인될 때만 계산하며 전출월부터 관계 점수를 코칭 근거에 쓰지 않는다.
- 등록된 현재·과거 학생 이름, 이메일, 휴대전화번호를 가린다. 모든 제3자 개인정보를 자동으로 완벽히 제거하는 기능은 아니므로 교사 기록에도 불필요한 개인정보를 입력하지 않는다.
- OpenAI Responses API, gpt-5.6-terra, reasoning low, store:false. 응답 모양은 JSON Schema로 고정하고 근거 ID·한국어 표현을 서버에서 추가 검증한다.
- 성격 유형·진단·숨은 감정·원인을 단정하지 않는다. 자료가 적으면 확인 질문 위주로 제안하며, 의미 있는 근거가 없으면 AI 생성 없이 안내만 표시한다.
- 자료 또는 명단이 변경되면 저장 카드를 숨기고 수동 갱신을 안내한다. 생성 중 자료가 바뀌면 저장을 거절한다.
- 코칭 카드와 적용 결과는 담당 교사만 접근한다. 전출 학생은 기존의 전출 학생 포함 옵션으로 조회한다.
- 학급당 한국 시간 월 10회 생성 요청(실패 포함). 기존 학급/관계 AI 분석의 제한과 별도이다. 캐시 조회는 차감하지 않는다.
- 카드 삭제는 결과 내용과 적용 결과를 제거하되 요청 횟수·감사 기록은 남긴다. 학생 설문·관찰 기록은 삭제하지 않는다.

## 배포와 검증

- DB: `supabase/migrations/20260909150000_student_coaching_cards.sql` 별도 적용 필요.
- 함수: `student-coaching`. 기존 `analyze-class`는 변경하지 않는다.
- DB 검증: `tests/student-coaching.integration.sql`. 가상 학급과 자료만 만들고 반드시 롤백한다.
- Node 검증: 인증·권한·자료 부족·원문 범위·가명 처리·저장 재사용·근거 ID·오류 처리를 모의 API로 검증한다.
- 화면 검증: 360/768/1440px에서 생성·근거·저장·학생 전환·삭제를 모의 API로 검증한다. 실학생 자료를 외부 AI에 전송하는 검증은 하지 않는다.

## 후속 범위

학생 기록 PDF에 코칭 카드 포함, 기존 학급 백업 형식에 새 카드·적용 결과 포함, 보존 기한별 일괄 정리는 별도 후속 작업이다. 학급 자체 삭제에는 외래 키 연쇄 삭제가 적용된다. 본 기능의 코칭 효과나 심리측정 타당도를 검증했다고 주장하지 않는다.

공식 API 참고: https://developers.openai.com/api/docs/guides/structured-outputs
