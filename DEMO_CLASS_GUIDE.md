# 기능 점검용 데모 학급 사용 안내

실제 학생 자료 없이 시험용 교사 계정으로 주요 구현 화면을 확인하기 위한 비식별 데이터 세트다. 학생 이름과 응답은 모두 가상이며 실제 학생 판단이나 연수 자료로 사용하지 않는다.

## 현재 실험실 (2026-09-15)

대상은 `applicant-test@hhj3839.dev`의 `demo-aa891ab949014621`이다. 현재 가상 학생 10명이며 실험실 로그인과 가상 학급 둘러보기에서 같은 자료를 조회한다.

가상전출·가상새봄은 사용자 요청으로 삭제했다. `enrich_applicant_lab.sql`은 폐기 안내만 하는 파일이므로 다시 실행해도 학생·응답·AI 날짜가 변경되지 않는다. 아래 생성 절차는 **새 격리 시험 학급을 준비할 때만** 사용한다. 현재 실험실을 초기화하기 위해 실행하지 않는다.

- 게스트: 설문·학생별 변화·관계망·저장 AI·PDF 조회와 한도 내 새 AI 생성.
- 교사: 기존 관리 기능 유지. 게스트는 수정·삭제·설문 링크 접근 불가.
- 게스트 공용 월 학급·관계 AI 합산 10회, 코칭 100회 상한. 실험실 계정의 코칭 한도도 적용된다.
- 코칭 2번 관계 고민, 4번 즉시 도움 등 가상 사례를 비교한다.
- 가상 자료 월 갱신은 실험실 교사 로그인 시 실행한다. AI 생성·완료 시각과 기준 월은 이동하지 않는다.
- 새달 결과가 없으면 저장 결과 없음으로 표시하며 자동 유료 생성하지 않는다.
- 과거 날짜가 이미 이동된 결과는 원본 확인 없이 임의 복구하지 않는다.

물리 태블릿의 QR·동시 제출·통신 복구는 현장 검증이 남아 있다. 실제 개인정보를 공개 실험실에 입력하지 않는다.

## 생성

1. Supabase Dashboard의 **SQL Editor → New query**를 연다.
2. `supabase/demo/seed_demo_class.sql` 전체를 붙여 넣는다.
3. 첫 부분의 `REPLACE_WITH_TEST_TEACHER_EMAIL`을 기존 시험용 교사 이메일로 바꾼다.
4. **Run**을 누른다.
5. 시험용 교사 계정으로 웹앱에 로그인하고 `기능 점검 교사 · 5학년 99반` 학급을 선택한다.

스크립트를 다시 실행하면 해당 계정의 고정 `demo-*` 학급만 초기화한다. 같은 교사의 다른 운영 학급은 변경하지 않는다.

자동 최신화 마이그레이션 적용 후에는 실험실 계정으로 로그인할 때 데모 학급의 최근 설문 월을 현재 달에 맞춘다. 따라서 SQL을 매달 다시 실행하지 않아도 최근 12개월 동안 매달 설문을 실시한 것처럼 제출 현황·학생별 변화·관계 분석 예시가 이어진다. 이 동작은 로그인한 교사가 소유한 `demo-*` 학급에만 허용된다.

## 포함된 점검 상황

- 가상 학생 10명과 최근 12개월 응답
- 최신 달 미제출 학생 1명과 추가 제출 1건
- 분석 제외 응답 1건
- 직접 도움 요청, 이번 주 대화 요청, 종결된 확인 기록
- 설문이 없는 학생의 진행 중 확인·지원 기록
- 관계 점수 상승·하락과 구조화된 긍정 언급
- 세 개의 느슨한 관계 묶음, 묶음 사이 연결 학생, 비대칭 관계와 친구별 응답 편차
- 최근 12개월 누적 응답과 마지막 3개월 동안 강화·약화되는 관계
- API 호출 없이 확인 가능한 저장 AI 분석 결과 1건
- 진행 중 및 완료된 관찰 기록

## 화면별 빠른 확인

| 화면 | 확인할 내용 |
|---|---|
| 설문 관리 | 최신 달 제출 9/10명, 미제출 윤새봄, 오가람 추가 제출 |
| 학급 분석 | 저장된 AI 결과, 먼저 살펴볼 학생 3명, 학급 패턴 |
| 학생별 살펴보기 | 김하늘 상승, 이하람 변화, 윤새봄 무응답+지원 기록 |
| 관계 분석 | 세 관계 묶음, 묶음 사이 연결, 제한된 연결과 월별 변화가 섞인 12개월 누적 관계망 |
| PDF | 학급 AI 분석과 학생 기록 다운로드 |
| 권한 | 다른 시험 교사 계정에서 이 학급이 보이지 않는지 확인 |

AI의 `학생-4` 같은 익명 번호는 교사 화면에서 가상 학생 이름으로 치환된다. 새 분석을 누르면 실제 API 사용량에 포함되므로 저장 결과 확인만 필요할 때는 누르지 않는다.

## 학년 말 자료 정리 재시험

전년도 비식별 데모 학급에서만 다음 순서로 확인한다.

1. `학급 설정 → 학년 말 자료 정리`를 연다.
2. `학년 말 보고서 PDF 만들기`를 눌러 파일이 생성되는지 확인한다.
3. 별도의 보관 확인이나 삭제 대상 미리보기 없이 학급 이름·확인 문구 입력란이 바로 활성화되는지 확인한다.
4. 학급 이름과 `전년도 학급 자료 삭제`를 입력하고 복구 불가능 안내를 확인한다.
5. 영구 삭제 후 학생 개인정보가 사라지고 최소 감사 기록만 남는지 확인한다.
6. `supabase/demo/seed_demo_class.sql`을 다시 실행해 데모 학급이 정상 복원되는지 확인한다.

현재 학년도 또는 운영 학급에서는 이 시험을 실행하지 않는다.

## 삭제

`supabase/demo/reset_demo_class.sql`의 이메일을 같은 시험용 교사 이메일로 바꾸고 SQL Editor에서 실행한다. 해당 시험용 계정의 고정 데모 학급과 종속 데이터만 삭제된다.

운영 학급을 삭제 대상으로 직접 입력하는 방식은 제공하지 않는다.

