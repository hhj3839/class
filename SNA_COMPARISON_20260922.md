# 관계 해석 1차 보완 — 2026-09-22

- 누적 평균 지도와 메뉴는 유지한다. 관계별 근거에서 같은 달 양방향 관측 개월 수를 분모로 서로 4점 이상인 개월 수를 설명한다.
- 각 방향의 관측 시기가 겹치지 않으면 서로 다른 시기의 응답이라고 표시한다. 미응답은 상호 관계 없음으로 바꾸지 않는다.
- 관계 AI v16은 화면의 `relationship-changes.js`를 재사용한다. 달을 건너뛰지 않고 직전 달만 비교한다.
- 두 달 모두 양방향 점수와 동일 학생 ID가 확인된 쌍만 새 연결·유지·기준 미충족으로 계산한다. 전출한 달부터 비교를 보류한다.
- 월별 전체 수는 해당 달 기술 통계로 유지하되, 그 차이를 관계 변화로 해석하지 않도록 AI 지침과 별도의 비교 입력을 제공한다.
- 기존 AI 카드는 생성 당시 버전을 유지하며 자동 재생성하지 않는다. 새 기준은 사용자가 AI 새 분석을 선택할 때 적용된다.

## 범위와 검증

- DB·학생 원문 변경 없음. 유료 API 호출 없이 가상 API 요청 본문과 응답을 검증한다.
- 자동 계산/회귀 시험 301개와 360/768/1440px 브라우저 시험을 실행한다.
- 학생 PDF·학생 코칭 계산을 새로 확장하지 않았다. 이번 변경은 관계 화면과 관계 AI의 비교 일치에 집중한다.
- 전입일은 현재 비교에 반영하지 않는다. 정확한 재적 기간별 분모 보정, 집단 기준 민감도, 실제 학교 현장 검증은 별도 과제다.
- 4점/4.5점 및 80%는 운영 기준이며 연구로 검증된 진단 기준이 아니다.
