const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),files=[];function walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){if(['.git','node_modules'].includes(entry.name))continue;const full=path.join(dir,entry.name);if(entry.isDirectory())walk(full);else if(/\.(js|ts|html|md|sql|json|css)$/.test(entry.name))files.push(full)}}walk('.');test('공개 저장소에는 GitHub·OpenAI 비밀키 원문이 없다',()=>{assert.deepEqual(files.filter(file=>/ghp_[A-Za-z0-9]{20,}|sk-proj-[A-Za-z0-9_-]{20,}/.test(fs.readFileSync(file,'utf8'))),[])});
